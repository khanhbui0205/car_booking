<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Car Booking System</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-light">

<header>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold fs-5" href="/dashboard">
        <i class="fas fa-car"></i> Car Booking System
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/dashboard">
            <i class="fas fa-home"></i> Dashboard
          </a>
          <a class="nav-link" href="/ui/cars">
            <i class="fas fa-car"></i> Cars
          </a>
          <a class="nav-link" href="/ui/bookings">
            <i class="fas fa-calendar-alt"></i> Bookings
          </a>
          <% if (user.role === 'admin') { %>
            <a class="nav-link" href="/ui/users">
              <i class="fas fa-users"></i> Users
            </a>
          <% } %>
          <div class="nav-divider" style="width: 1px; height: 25px; background: rgba(255,255,255,0.3); margin: 0 15px;"></div>
          <span class="nav-link" style="color: rgba(255, 255, 255, 0.9);">
            <i class="fas fa-user-circle"></i> <%= user.name %>
          </span>
          <a class="nav-link" href="/auth/logout" style="color: #ff6b6b !important;">
            <i class="fas fa-sign-out-alt"></i> Logout
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<main>
  <div class="container" style="max-width: 1200px;">
    <!-- Welcome Section -->
    <div class="row mb-5">
      <div class="col-lg-8">
        <h1 class="display-5 fw-bold" style="color: #0066cc; margin-bottom: 10px;">
          Welcome back, <%= user.name %>! 👋
        </h1>
        <p style="color: #666; font-size: 1.1rem;">
          Manage your car rentals and bookings efficiently
        </p>
      </div>
      <div class="col-lg-4 text-end">
        <div style="background: linear-gradient(135deg, #e6f2ff 0%, #cce5ff 100%); padding: 20px; border-radius: 12px;">
          <p style="margin: 0; color: #0066cc; font-weight: 600; font-size: 0.9rem;">USER ROLE</p>
          <p style="margin: 0; font-size: 1.5rem; font-weight: 800; color: #004a99;">
            <i class="<%= user.role === 'admin' ? 'fas fa-crown' : 'fas fa-user' %>"></i> 
            <%= user.role === 'admin' ? 'Administrator' : 'Regular User' %>
          </p>
        </div>
      </div>
    </div>

    <!-- Main Action Cards -->
    <div class="row">
      <!-- Cars Card -->
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card shadow-lg border-0 h-100" style="transition: all 0.3s ease; cursor: pointer;">
          <div style="background: linear-gradient(135deg, #0066cc 0%, #1a7fd9 100%); padding: 30px; text-align: center; color: white;">
            <i class="fas fa-car" style="font-size: 3rem; margin-bottom: 15px;"></i>
            <h3 style="margin: 0;">Browse Cars</h3>
          </div>
          <div class="card-body text-center">
            <p style="color: #666; margin: 15px 0;">
              Browse and rent from our collection of available cars
            </p>
            <a href="/ui/cars" class="btn btn-primary w-100">
              <i class="fas fa-search me-2"></i> View Cars
            </a>
          </div>
        </div>
      </div>

      <!-- Bookings Card -->
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card shadow-lg border-0 h-100" style="transition: all 0.3s ease; cursor: pointer;">
          <div style="background: linear-gradient(135deg, #1a7fd9 0%, #00a8e8 100%); padding: 30px; text-align: center; color: white;">
            <i class="fas fa-calendar-check" style="font-size: 3rem; margin-bottom: 15px;"></i>
            <h3 style="margin: 0;">My Bookings</h3>
          </div>
          <div class="card-body text-center">
            <p style="color: #666; margin: 15px 0;">
              View and manage your current bookings
            </p>
            <a href="/ui/bookings" class="btn btn-primary w-100">
              <i class="fas fa-list me-2"></i> View Bookings
            </a>
          </div>
        </div>
      </div>

      <!-- User Management Card (Admin Only) -->
      <% if (user.role === 'admin') { %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card shadow-lg border-0 h-100" style="transition: all 0.3s ease; cursor: pointer;">
            <div style="background: linear-gradient(135deg, #00a8e8 0%, #00c9ff 100%); padding: 30px; text-align: center; color: white;">
              <i class="fas fa-users-cog" style="font-size: 3rem; margin-bottom: 15px;"></i>
              <h3 style="margin: 0;">User Management</h3>
            </div>
            <div class="card-body text-center">
              <p style="color: #666; margin: 15px 0;">
                Manage system users and their roles
              </p>
              <a href="/ui/users" class="btn btn-primary w-100">
                <i class="fas fa-cog me-2"></i> Manage Users
              </a>
            </div>
          </div>
        </div>
      <% } %>
    </div>

    <!-- Info Section -->
    <div class="row mt-5">
      <div class="col-lg-12">
        <div style="background: linear-gradient(135deg, #f0f5ff 0%, #e6f0ff 100%); padding: 30px; border-radius: 12px; border-left: 4px solid #0066cc;">
          <h4 style="color: #0066cc; margin-bottom: 15px;">
            <i class="fas fa-info-circle me-2"></i> Getting Started
          </h4>
          <ul style="margin: 0; color: #666; line-height: 1.8;">
            <li><strong>Browse Cars:</strong> Explore our collection of available vehicles</li>
            <li><strong>Make a Booking:</strong> Select your desired car and dates to make a booking</li>
            <li><strong>Track Bookings:</strong> View all your bookings and their status</li>
            <% if (user.role === 'admin') { %>
              <li><strong>Manage System:</strong> As an admin, you can manage users and view all bookings</li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</main>

<footer class="bg-dark text-white text-center py-4 mt-5">
  <p class="mb-0"><i class="fas fa-copyright"></i> 2026 – SDN302 Car Booking System</p>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
  // Add hover effect to cards
  document.querySelectorAll('.card').forEach(card => {
    card.addEventListener('mouseenter', function() {
      this.style.transform = 'translateY(-10px)';
      this.style.boxShadow = '0 12px 40px rgba(0, 102, 204, 0.2)';
    });
    card.addEventListener('mouseleave', function() {
      this.style.transform = 'translateY(0)';
      this.style.boxShadow = '0 4px 15px rgba(0, 102, 204, 0.08)';
    });
  });
</script>
</body>
</html>
