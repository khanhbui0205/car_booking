<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Bookings - Car Booking System</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-light">

<header>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold fs-5" href="/dashboard">
        <i class="fas fa-car"></i> Car Booking System
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/dashboard">
            <i class="fas fa-home"></i> Dashboard
          </a>
          <a class="nav-link" href="/ui/cars">
            <i class="fas fa-car"></i> Cars
          </a>
          <a class="nav-link active" href="/ui/bookings">
            <i class="fas fa-calendar-alt"></i> Bookings
          </a>
          <% if (user && user.role === 'admin') { %>
            <a class="nav-link" href="/ui/users">
              <i class="fas fa-users"></i> Users
            </a>
          <% } %>
          <div class="nav-divider" style="width: 1px; height: 25px; background: rgba(255,255,255,0.3); margin: 0 15px;"></div>
          <a class="nav-link" href="/auth/logout" style="color: #ff6b6b !important;">
            <i class="fas fa-sign-out-alt"></i> Logout
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<main>
  <div class="container">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h3>
        <i class="fas fa-calendar-alt"></i> My Bookings
      </h3>
      <a href="/ui/bookings/create" class="btn btn-primary">
        <i class="fas fa-plus-circle me-2"></i> New Booking
      </a>
    </div>

    <!-- Bookings Table/Cards -->
    <div class="card card-custom">
      <table class="table table-hover mb-0">
        <thead>
          <tr>
            <th>
              <i class="fas fa-user me-2"></i> Guest
            </th>
            <th>
              <i class="fas fa-car me-2"></i> Vehicle
            </th>
            <th>
              <i class="fas fa-calendar-check me-2"></i> Dates
            </th>
            <th>
              <i class="fas fa-money-bill me-2"></i> Total Price
            </th>
            <th>
              <i class="fas fa-info-circle me-2"></i> Status
            </th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (bookings && bookings.length > 0) { %>
            <% bookings.forEach(b => { %>
              <tr>
                <td>
                  <div style="display: flex; align-items: center; gap: 10px;">
                    <div style="width: 40px; height: 40px; background: linear-gradient(135deg, #0066cc 0%, #1a7fd9 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-weight: 700;">
                      <%= b.userId.name.charAt(0).toUpperCase() %>
                    </div>
                    <div>
                      <strong><%= b.userId.name %></strong><br>
                      <small style="color: #666;"><%= b.userId.email %></small>
                    </div>
                  </div>
                </td>
                <td>
                  <strong><%= b.carId.brand %></strong><br>
                  <span style="color: #666;"><%= b.carId.model %></span>
                </td>
                <td>
                  <div style="font-size: 0.9rem;">
                    <i class="fas fa-calendar-check me-1" style="color: #0066cc;"></i>
                    <%= new Date(b.startDate).toLocaleDateString('vi-VN') %><br>
                    <i class="fas fa-calendar-times me-1" style="color: #0066cc;"></i>
                    <%= new Date(b.endDate).toLocaleDateString('vi-VN') %>
                  </div>
                </td>
                <td>
                  <div style="font-weight: 800; color: #0066cc; font-size: 1.1rem;">
                    <%= b.totalPrice %>đ
                  </div>
                </td>
                <td>
                  <% if (b.status === "ACTIVE") { %>
                    <span class="badge badge-active">
                      <i class="fas fa-hourglass-end me-1"></i> ACTIVE
                    </span>
                  <% } else if (b.status === "CONFIRMED") { %>
                    <span class="badge badge-confirmed">
                      <i class="fas fa-check-circle me-1"></i> CONFIRMED
                    </span>
                  <% } else { %>
                    <span class="badge badge-cancelled">
                      <i class="fas fa-times-circle me-1"></i> CANCELLED
                    </span>
                  <% } %>
                </td>
                <td>
                  <a href="/ui/bookings/<%= b._id %>" class="btn btn-sm btn-outline-primary">
                    <i class="fas fa-eye me-1"></i> Details
                  </a>
                </td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr>
              <td colspan="6" style="text-align: center; padding: 60px 20px;">
                <i class="fas fa-inbox" style="font-size: 3rem; color: #ccc; margin-bottom: 20px; display: block;"></i>
                <h5 style="color: #999; margin-bottom: 10px;">No Bookings Yet</h5>
                <p style="color: #bbb; margin-bottom: 20px;">You haven't made any bookings. Start by browsing our cars!</p>
                <a href="/ui/cars" class="btn btn-primary">
                  <i class="fas fa-search me-2"></i> Browse Cars
                </a>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</main>

<footer class="bg-dark text-white text-center py-4 mt-5">
  <p class="mb-0"><i class="fas fa-copyright"></i> 2026 – SDN302 Car Booking System</p>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
