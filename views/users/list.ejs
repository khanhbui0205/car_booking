<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Management - Car Booking System</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-light">

<header>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold fs-5" href="/dashboard">
        <i class="fas fa-car"></i> Car Booking System
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/dashboard">
            <i class="fas fa-home"></i> Dashboard
          </a>
          <a class="nav-link" href="/ui/cars">
            <i class="fas fa-car"></i> Cars
          </a>
          <a class="nav-link" href="/ui/bookings">
            <i class="fas fa-calendar-alt"></i> Bookings
          </a>
          <% if (user && user.role === 'admin') { %>
            <a class="nav-link active" href="/ui/users">
              <i class="fas fa-users"></i> Users
            </a>
          <% } %>
          <div class="nav-divider" style="width: 1px; height: 25px; background: rgba(255,255,255,0.3); margin: 0 15px;"></div>
          <a class="nav-link" href="/auth/logout" style="color: #ff6b6b !important;">
            <i class="fas fa-sign-out-alt"></i> Logout
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<main>
  <div class="container">
    <!-- Header -->
    <div class="mb-4">
      <h3>
        <i class="fas fa-users"></i> User Management
      </h3>
      <p style="color: #666; margin-bottom: 0;">View and manage all system users</p>
    </div>

    <!-- Users Table -->
    <div class="card card-custom">
      <table class="table table-hover mb-0">
        <thead>
          <tr>
            <th>
              <i class="fas fa-user me-2"></i> Name
            </th>
            <th>
              <i class="fas fa-envelope me-2"></i> Email
            </th>
            <th>
              <i class="fas fa-shield-alt me-2"></i> Role
            </th>
            <th>
              <i class="fas fa-calendar-alt me-2"></i> Created
            </th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (users && users.length > 0) { %>
            <% users.forEach(u => { %>
              <tr>
                <td>
                  <div style="display: flex; align-items: center; gap: 12px;">
                    <div style="width: 40px; height: 40px; background: linear-gradient(135deg, #0066cc 0%, #1a7fd9 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-weight: 700; font-size: 0.9rem;">
                      <%= u.name.charAt(0).toUpperCase() %>
                    </div>
                    <strong><%= u.name %></strong>
                  </div>
                </td>
                <td><%= u.email %></td>
                <td>
                  <% if (u.role === "admin") { %>
                    <span class="badge badge-confirmed">
                      <i class="fas fa-crown me-1"></i> ADMIN
                    </span>
                  <% } else { %>
                    <span class="badge badge-active">
                      <i class="fas fa-user-check me-1"></i> USER
                    </span>
                  <% } %>
                </td>
                <td>
                  <small style="color: #666;">
                    <%= new Date(u.createdAt).toLocaleDateString('vi-VN') %>
                  </small>
                </td>
                <td>
                  <button class="btn btn-sm btn-outline-primary" disabled>
                    <i class="fas fa-edit me-1"></i> Edit
                  </button>
                </td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr>
              <td colspan="5" style="text-align: center; padding: 60px 20px;">
                <i class="fas fa-inbox" style="font-size: 3rem; color: #ccc; margin-bottom: 20px; display: block;"></i>
                <h5 style="color: #999;">No Users Found</h5>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <!-- Stats -->
    <div class="row mt-5">
      <div class="col-md-4">
        <div class="card border-0 shadow-lg" style="background: linear-gradient(135deg, #e6f2ff 0%, #cce5ff 100%); border-left: 4px solid #0066cc;">
          <div class="card-body p-4" style="text-align: center;">
            <p style="margin: 0; color: #666; font-size: 0.85rem; font-weight: 600; text-transform: uppercase;">Total Users</p>
            <p style="margin: 10px 0 0 0; font-size: 2.5rem; font-weight: 800; color: #0066cc;">
              <%= users ? users.length : 0 %>
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card border-0 shadow-lg" style="background: linear-gradient(135deg, #fff5e6 0%, #ffe6cc 100%); border-left: 4px solid #ff9800;">
          <div class="card-body p-4" style="text-align: center;">
            <p style="margin: 0; color: #666; font-size: 0.85rem; font-weight: 600; text-transform: uppercase;">Admins</p>
            <p style="margin: 10px 0 0 0; font-size: 2.5rem; font-weight: 800; color: #ff9800;">
              <%= users ? users.filter(u => u.role === 'admin').length : 0 %>
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card border-0 shadow-lg" style="background: linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%); border-left: 4px solid #4caf50;">
          <div class="card-body p-4" style="text-align: center;">
            <p style="margin: 0; color: #666; font-size: 0.85rem; font-weight: 600; text-transform: uppercase;">Regular Users</p>
            <p style="margin: 10px 0 0 0; font-size: 2.5rem; font-weight: 800; color: #4caf50;">
              <%= users ? users.filter(u => u.role === 'user').length : 0 %>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>

<footer class="bg-dark text-white text-center py-4 mt-5">
  <p class="mb-0"><i class="fas fa-copyright"></i> 2026 – SDN302 Car Booking System</p>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
